"""Experiment builders.

The core SURE/PURE and conformal code in :mod:`sscp.losses` and
:mod:`sscp.conformal` is independent of DeepInv. The functions in this module are
thin optional wrappers for reproducing the paper experiments.
"""
